{
  "name": "@piccolo/ext-templates",
  "version": "0.0.0",
  "private": true,
  "description": "ext-templates: Prompt templates extension Worker",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "vitest": "*",
    "wrangler": "*"
  }
}
