{
  "name": "@piccolo/ext-d1-tool",
  "version": "0.0.0",
  "private": true,
  "description": "ext-d1-tool: D1 database tool extension Worker",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "*",
    "vitest": "*",
    "wrangler": "*"
  }
}
