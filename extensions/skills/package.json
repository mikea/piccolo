{
  "name": "@piccolo/ext-skills",
  "version": "0.0.0",
  "private": true,
  "description": "ext-skills: Agent skills extension Worker",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "wrangler deploy --dry-run --config wrangler.template.jsonc",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "@vitest/coverage-istanbul": "^4.1.1",
    "vitest": "*",
    "wrangler": "^4.78.0"
  }
}
