{
  "name": "@piccolo/ext-fetch-tool",
  "version": "0.0.0",
  "private": true,
  "description": "ext-fetch-tool: HTTPS GET/HEAD fetch tool extension Worker",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "wrangler deploy --dry-run --config wrangler.template.jsonc",
    "deploy": "wrangler deploy",
    "types": "wrangler types src/worker-configuration.d.ts"
  },
  "dependencies": {
    "@piccolo/api": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "@piccolo/core": "workspace:*",
    "@vitest/coverage-istanbul": "^4.1.1",
    "vitest": "*",
    "wrangler": "*"
  }
}
