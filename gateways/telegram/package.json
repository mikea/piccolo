{
  "name": "@piccolo/telegram-gateway",
  "version": "0.0.0",
  "private": true,
  "description": "piccolo-telegram-gateway: Telegram bot gateway Worker",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "wrangler deploy --dry-run --config wrangler.template.jsonc",
    "deploy": "wrangler deploy",
    "types": "wrangler types src/worker-configuration.d.ts --config wrangler.template.jsonc",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "@piccolo/api": "workspace:*",
    "grammy": "^1.41.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "vitest": "*",
    "wrangler": "^4.78.0"
  }
}
