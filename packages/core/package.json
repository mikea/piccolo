{
  "name": "@piccolo/core",
  "version": "0.0.0",
  "private": true,
  "description": "piccolo-core: session orchestration Worker and JSRPC hub",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "*",
    "vitest": "*",
    "wrangler": "*"
  }
}
